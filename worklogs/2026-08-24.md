# Worklog 2026-08-24

- Fixed GitLab backend integration tests by initializing MongoDB test users in CI.
- Split CI test stages into `test (unit)` and `test (integration)`.
- Configured `main` to run the complete backend test suite before deployment.
- Removed the legacy Flask application and migrated runtime, Docker, CLI, and documentation paths to FastAPI/ASGI.
- Replaced Flask CLI commands with `python -m morpheus.cli`.
- Improved dependency constraints and regenerated `uv.lock`.
- Upgraded FloPy from 3.7 to 3.10.0.
- Fixed FloPy `ModelTime` parsing by passing a Python `datetime` instead of a timezone-bearing string.
- Verified 38 FloPy MODFLOW wrapper and mapper tests successfully.
- Fixed deployment by using the `morpheus` container user and copying the updated suite Compose file to the server.
- Added post-deployment cleanup for Compose orphans and dangling Docker images.
- Pushed the changes to GitLab `dev` and `main`.
