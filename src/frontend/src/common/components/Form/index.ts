import Form, {Checkbox, Dropdown, FormField, Group, Input, Label, Radio} from './Form';

const FormComponent = {
  Form,
  FormField,
  Input,
  Checkbox,
  Dropdown,
  Label,
  Radio,
  Group,
};

export default FormComponent;
