import React from 'react';
import {PageTitle} from '../components';

const Home = () => (
  <>
    <PageTitle
      title={'Home'}
      description={'Welcome to the home page'}
    />
  </>
);

export default Home;
