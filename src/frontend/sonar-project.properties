# must be unique in a given SonarQube instance
sonar.projectKey=ultron
sonar.qualitygate.wait=true

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=./src
sonar.test=**/*test.ts, **/*test.tsx, **/*spec.ts, **/*spec.tsx
sonar.coverage.exclusions=**/*test.ts, **/*test.tsx, **/*spec.ts, **/*spec.tsx, **/*.stories.tsx, **/*.stories.ts, **/*.stories.jsx, **/*.d.ts, **/*.d.tsx

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
sonar.host.url=https://sonarqube.inowas.com
sonar.javascript.lcov.reportPaths=./coverage/lcov.info
